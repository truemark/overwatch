// TODO
import {Construct} from 'constructs';

export interface OverwatchSupportProps {
  readonly somethingsomething: string;
}

export class OverwatchSupport extends Construct {
  constructor(scope: Construct, id: string, props: OverwatchSupportProps) {
    super(scope, id);
  }
}
