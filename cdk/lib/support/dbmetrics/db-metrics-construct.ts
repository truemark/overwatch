import {Construct} from 'constructs';

// TODO Remove when props is populated
// eslint-disable-next-line @typescript-eslint/no-empty-interface
export interface DbMetricsConstructProps {
  // TODO Add properties
}

export class DbMetricsConstruct extends Construct {
  // TODO Remove when props is used
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  constructor(scope: Construct, id: string, props: DbMetricsConstructProps) {
    super(scope, id);

    // TODO Add construct code
  }
}
